#include <stdio.h>
int main(void)
{
    int appleBox = 30;
    int grapeBox = 20;
    int total;

    total = appleBox + grapeBox;

    printf("There are %d boxes in total \n", total);

    return 0;
}

// There are 50 boxes in total 